# Issue 棚卸し 2026-09-03 — 結果と、見えたクラスタ

**Issue**: #689 / **対象**: open issue 164 件（うち 31 日以上更新なし 106 件）

> **この文書の役割**: 何を閉じ、何を残し、**なぜそう判断したか**を残す。
> 加えて、1 件ずつ読んだ結果見えてきた **issue のクラスタ**（同じ設計で一緒に扱うべき群）を記録する。
> クラスタの方が、個別の issue より設計の入力として価値がある。

---

## 1. 結果

**164 → 120 件**（44 件処理）。

| 分類 | 件数 | 代表例 |
|---|---|---|
| **実装完了** | 15 | #95（VST3/CLAP ホスト実装済み）/ #107（daemon+WebSocket）/ #232（MCP が 3→30 ツールへ）/ #331（A4-2b-2b N-channel） |
| **前提が消滅** | 12 | #94（Tauri 不採用）/ #233-#235・#242（藝大不採択）/ #201・#210・#211（SC 経路・#502 退役予定） |
| **後継へ集約** | 8 | #379→#656 / #105→#670 / #96→#615 / #151→#662 / #224・#240→#413 |
| **残す（証拠付き）** | 9 | #92 / #125-#130 / #156 / #197 |

## 2. 🔴 最も古い issue が、最も正しかった

**#218**（2026-05-09・WORK_LOG ローテーションの自動化が無い）は、こう予測していた:

> 閾値超過に気づかないまま commit が積み重なる可能性 / 気づいた時点で archive 対象量が膨大化

**そのとおりになった。** 2026-09-02 時点で **14,926 行 / 1,221 KB**（規則の 7.5 倍）。

しかも本日 main が**同じ問題を #686 として重複起票**している（起票前の既存確認を怠った）。

**タイトルだけ見れば #218 は「古い chore」だった。** 更新日での判定は、この種の issue を必ず取り逃す。

## 3. 判定が変わった例

**#92（タイムストレッチ選定）** — `rubato` が入っているので「完了」に見える。
だが **rubato はリサンプラ**であって、`fixpitch()` が要求する**ピッチ保持のストレッチではない**。
#213 が未実装のまま残っており、**選定はまだ済んでいない**。

加えて本文は「Steam / App Store 販売」を前提にライセンスを論じているが、現在の配布形態は
Marketplace + OrbitStudio.app。**比較の観点自体を更新する必要がある。**

---

## 4. 見えたクラスタ（設計の入力）

個別に着手すると同じ設計を何度もやり直すことになる群。

### C1. 診断の整合 — 「何が書けて何が書けないか」

| issue | 症状 |
|---|---|
| **#280** | `seq.root(C)` が runtime で throw（spec と実装の乖離。`sequence.ts:913` / `parse-expression.ts:813`） |
| **#644** | シーケンス種別が使えない値を setter が黙って記録する |
| **#610** | 拡張の診断が、エンジンの拒否する構文を受理する |
| **#255** | unbound-name の slot 意味論・空パターン束縛の診断 |

**共通の問い**: 「書けるが効かない」「書けるはずが弾かれる」をどう伝えるか。
**バラバラに直すと診断の方針が場所ごとに割れる。**

### C2. プラグインの生存管理

| issue | 症状 |
|---|---|
| **#418** | respawn 後の resume-point ハンドシェイクが無い |
| **#626** | watchdog に見捨てられたプラグインが、同じ spec を再宣言しても復旧しない |
| **#637** | クラッシュしやすいプラグインを隔離せず、チェーン全体を巻き込む |
| **#342** | CLAP host の teardown / install-ring drain（前提が #628 のラックで変わっている） |

### C3. daemon 起動の失敗面

| issue | 症状 |
|---|---|
| **#129** | `DaemonNotFoundError` が `searchedPaths` を公開しない（隣の `DaemonStartupError` は公開している） |
| **#383** | explicit / env の指定が無効でも**黙って別のバイナリへ落ちる** |
| **#130** | `__dirname` からの `../` 依存（`daemon-client.ts:228,248`）がビルド構造に暗黙依存 |
| **#367** | 起動失敗の情報面（部分的に実装済み） |

**#654（hoist でランタイム依存が vsix から抜け、初回評価で落ちた）はこのクラスタの実害。**

### C4. 時間の粒度

| issue | 内容 |
|---|---|
| **#428** | ノートが `PlayAt` ではなく即時メソッドのまま（`engine_wrap.rs:4455` に明記） |
| **#680** | プラグインのパラメータを DSL から動かす（**CLAP も VST3 もサンプルオフセットを持つ**） |
| **#674** | シーケンスから OSC を送る |

🔴 **パラメータはサンプル精度で送れるのに、ノートが即時**という不整合が生まれる。**揃えて設計すべき。**

### C5. 配布

| issue | 内容 |
|---|---|
| **#656** `release-gate` | OrbitStudio.app の署名・notarize・リリース経路 |
| **#197** `release-gate` | `VSCE_PAT` / `OVSX_PAT` **未登録**（`gh secret list` で確認） |
| **#184** | Marketplace 公開（コード側は完成・**残りはアカウント作業のみ**） |
| **#385** | workspace trust policy 未設定（**配布したアプリで最初に躓く箇所になりうる**） |
| **#659** | ローカルリリースのスクリプト化（未決着） |
| **#321** | LinkAudio は GPL feature（default off）。**配布形態が未決で #656 と衝突しうる** |

### C6. ミキサーの出力側

| issue | 内容 |
|---|---|
| **#611** `foundation` | 任意のバス／シーケンスを任意のチャンネルへ |
| **#409** | sidechain / multi-out（**#611 が決まらないと形が決められない**） |
| **#647** | マルチティンバー（各パートが自分のバスへ） |
| **#598** | オフラインの per-bus レンダ |

---

## 5. ラベル

`foundation`（他の issue の前提）と `release-gate`（リリース前に必要）の 2 枚のみを新設した。
理由と運用は `docs/core/PROJECT_RULES.md` §1b。

**種別ラベルは足さない** — 164 件中 162 件がタイトルに接頭辞を持っており、二重管理は必ず腐る。
既存ラベルは 20% にしか付いておらず、期限を名前にしたもの（`icmc-blocker` 等）は
`legacy:` へ改名した。

## 6. 残っている棚卸し

- **8/4 以降の 80 件は未着手**（現在進行中の作業が中心なので、棚卸しの価値は低い）
- **#292**（Post-2.0 Epic）は「閉じてよい」と判断したが owner 判断待ち
- **#410 / #465 / #315** は**再現確認が必要**（対象が消滅している可能性）
