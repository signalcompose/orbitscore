# 各 PR が完了するとユーザーは何ができるか（2026-09 実装プラン・ユーザー視点）

`IMPLEMENTATION_PLAN_2026-09.md` §1 の PR ごとに「完了するとできること」を**ユーザー視点**で 1 行にした。実装の中身・依存・検証は plan 側。owner 2026-09-03 夕「各 PR が完了すると何が出来るのかユーザー視点で纏めて」への回答。

**凡例（見え方）**: 🎵 音・操作が変わる / 👀 見える / 🧱 土台・変化なし / 📄 仕様・文書。「何も変わらない」は正直にそう書く（次の PR の土台）。

## 段が終わると何ができるか（plan §3 の 1 文をユーザーの言葉で）

| 段 | 名前 | 終わるとできること |
|---|---|---|
| 段 0 | 安全網 | 既存の譜面が同じ音のまま、という事実が capture の数値で固定される。以後どの変更でも「音が変わった」を機械が検出する。ユーザーの操作は何も変わらない。 |
| 段 1 | must-fix | 演奏が壊れる 5 件が直る。`global.gain(-6)` が instrument にも効く。演奏中に engine が throw で止まらない。選んだオーディオデバイスから本当に音が出る。RUN の終端で音が止まる。untrusted なフォルダでも動く。 |
| 段 2 | 出口の一般化 | `kick.output(verb, thru: true, db: -12).output(master)` と書ける。master も aux も物理アウトも同じ書き方。`send` は dB。`pan` がバス上で効く。instrument の `outs:` でマルチアウト。 |
| 段 3 | ログ → リプレイ | OrbitStudio で演奏すると譜面の隣の `orbslog/` にログが自動で残り、`orbitscore replay <log>` が同じ音を同じ小節で鳴らす。`--until 57:1` で途中から続きを演奏できる。 |
| 段 4 | render | `mix.render("stems/%n_%v.wav")` で演奏しながら stem が書け、`orbitscore render` / `replay --render` が実時間より速く同じファイル群を作る。4ch 以上の WAV にも置ける。 |
| 段 5 | 可視化・設定・性能 | engine が掴んでいるデバイス・レート・バッファ・callback 統計・設定の実効値が Studio に出る。child がアイドルで CPU を食わない。プール上限を気にしなくてよい。 |
| 段 6 | リリースゲート連鎖 | note が transport 時刻どおりに届く。プラグインのパラメータをパターンで動かせる。並列ラックの位相が揃う。`layer()` が並列に走る。instrument ラック。標準 compressor/limiter を好きな位置に挿せる。 |
| 段 7 | プラグイン境界 → render P3 | OSC をパターンで送れる。reference と E2E カバレッジが語彙の登録から自動で導かれる。オフラインレンダにプラグインと instrument が乗る（#598 完了）。 |
| 段 8 | 配布 | 署名・公証済み OrbitStudio.app と `.vsix` が GitHub Releases に置かれ、node が無い Mac でも起動して音が出る。 |

## PR-O — 出口の一般化（doc 611）

| PR | 見え方 | 段 | 完了するとできること |
|---|---|---|---|
| PR-O0 | 🧱 土台・変化なし | 段 0 | 何も変わらない。既存 4 譜面の音が数値で固定され、以後「音が変わった」を機械が見つける。 |
| PR-O1 | 📄 仕様・文書 | 段 0 | 仕様に `output` がライン要素として載る。読める・引用できる。 |
| PR-O2 | 🎵 音・操作が変わる | 段 1 | `global.gain(-6)` が instrument にも効く。8ch@2048 で無音になる不具合が直る。master のフェーダーが DAW と同じ位置（ラックの後）で効く。 |
| PR-O3 | 🧱 土台・変化なし | 段 2 | 何も変わらない。daemon がバスのラインを丸ごと受け取れるようになる（次の PR の土台）。既存譜面は同じ音。 |
| PR-O4 | 🎵 音・操作が変わる | 段 2 | `kick.output(verb, thru: true, db: -12).output(master)` と書ける。master・aux・物理アウトが同じ書き方。`send` が dB。`pan` がバス上の L/R バランスとして instrument にも効く。同じ宛先に 2 回 `output` すると 2 要素として加算。mono 宛先で cue 送り。 |
| PR-O5 | 🎵 音・操作が変わる | 段 2 | instrument の `outs:` でユニットごとの出口を分けられる（マルチアウト）。 |
| PR-O6 | 🧱 土台・変化なし | 段 2 | 何も変わらない。旧 wire の撤去。 |

## PR-L — セッションログ → リプレイ（doc 694）

| PR | 見え方 | 段 | 完了するとできること |
|---|---|---|---|
| PR-L0 | 📄 仕様・文書 | 段 0 | セッションログ v2 の仕様が読める（置き場・フレーム・純度・replay CLI）。 |
| PR-L1a | 🧱 土台・変化なし | 段 3 | CLI（`ORBITSCORE_SESSION_LOG=1`）で試すと、ログが譜面の隣の `orbslog/` に出て、`code` にはユーザーの書いたテキストだけが残る。Studio からはまだ出ない。 |
| PR-L1b | 👀 見える | 段 3 | **OrbitStudio で演奏すると `orbslog/<譜面名>.<日時>.orbslog` が自動で残る**。設定 `orbitscore.sessionLog` で off にできる。untitled は `untitled.*`。（ここが #694 の完了） |
| PR-L2 | 🎵 音・操作が変わる | 段 3 | 1 回の選択が 1 まとまりで評価される。選択の途中に構文エラーがあれば**何も実行されない**（今は途中まで実行される）。ログも 1 選択 = 1 レコード。 |
| PR-L3 | 👀 見える | 段 3 | GLOBAL を複数書いても、全部のログに start / stop が残る。 |
| PR-L7 | 👀 見える | 段 3 | ログに評価の成功 / 失敗と診断、import した module の本文、MCP（エージェント）由来の印が残る。後から読んで「何が起きたか」が分かる。 |
| PR-L8 | 🎵 音・操作が変わる | 段 3 | 演奏中に tempo / beat を変えても位置 `bar:beat` が正しく進む。**tempo 変更直後の LOOP が次の小節頭できちんと入る**（今は境界を飛ばして待つ）。 |
| PR-L9 | 👀 見える | 段 3 | 演奏の開始 / 終了時点のプラグイン状態が `orbslog/<log>.states/` に残る。後日そのセッションを同じ音で再生できる。 |
| PR-L4 | 🎵 音・操作が変わる | 段 3 | **`orbitscore replay <log>` でそのセッションを同じ音・同じ小節で再演奏できる**。失敗した評価も同じように失敗する（因果の再現）。（ここが #241 忠実リプレイの完了） |
| PR-L5 | 🎵 音・操作が変わる | 段 3 | `replay <log> --until 57:1` で 57 小節目の頭まで一瞬で畳み込み、そこからライブを続けられる。57:1 で効く予定だった差し替えは効いた状態で始まる。`--realtime` なら実時間で止める。 |
| PR-L6 | 👀 見える | 段 3 | `replay <log> --verify` で、ライブと再生のイベント列の一致と、音声資産（sha256）が変わっていないかを検証できる。 |

## PR-R — render（doc 598）

| PR | 見え方 | 段 | 完了するとできること |
|---|---|---|---|
| PR-R0 | 📄 仕様・文書 | 段 0 | render エンドポイントの仕様が読める。数値 `output(n)` の退役が仕様に載る。 |
| PR-R4 | 🧱 土台・変化なし | 段 0 | 何も変わらない。時計の注入（オフライン driver の土台）。 |
| PR-R1 | 🎵 音・操作が変わる | 段 4 | `var stems = mix.render("stems/%n_%v.wav")` を宣言して `kick.output(stems)` と書ける（ファイルが出るのは PR-R3 から）。数値 `output(1..16)` は書けなくなる。 |
| PR-R2 | 🧱 土台・変化なし | 段 4 | 何も変わらない。daemon 側の render 受け口。 |
| PR-R3 | 🎵 音・操作が変わる | 段 4 | **演奏しながら stem が WAV に書ける**。`global.start()` で開き `stop()` で閉じ、次の start で版 `%v` が増える。 |
| PR-R5 | 🧱 土台・変化なし | 段 4 | 何も変わらない。`.orbs` / `.orbslog` を仮想クロックで畳む driver（内部）。 |
| PR-R6 | 🧱 土台・変化なし | 段 4 | 何も変わらない。daemon のオフライン session。 |
| PR-R7 | 🎵 音・操作が変わる | 段 4 | **`orbitscore render <orbs> --duration 4:00` / `replay <log> --render` で、実時間より速く同じ stem + master が書き出せる**（プラグインと instrument は PR-R8 から）。 |
| PR-R8 | 🎵 音・操作が変わる | 段 7 | オフラインレンダにプラグインと instrument が乗る。ライブと同じ音が faster-than-realtime で出る。（#598 完了） |
| PR-R9 | 🎵 音・操作が変わる | 段 4 | `mix.render(path, channels: 4)` と `output(quad, at: 3)` / `mono: true` で 4ch 以上の WAV にステレオ対やモノを置ける。サラウンド化は Logic 等で。 |

## PR-P — プラグイン境界（doc 672）

| PR | 見え方 | 段 | 完了するとできること |
|---|---|---|---|
| PR-P8 | 🧱 土台・変化なし | 段 0 | 何も変わらない。in-process WASM の遅延計測（標準プラグインの実装形式を決める材料）。 |
| PR-P0 | 📄 仕様・文書 | 段 7 | DSL Plugin / DSP Plugin の契約が読める。何をプラグインで書けるかが分かる。 |
| PR-P1 | 🧱 土台・変化なし | 段 7 | 何も変わらない。語彙が登録から導かれる。 |
| PR-P2 | 👀 見える | 段 7 | `reference/methods.md` が自動生成され、メソッドの記載漏れが出なくなる。 |
| PR-P3 | 🧱 土台・変化なし | 段 7 | 何も変わらない。E2E カバレッジが登録から導かれる。 |
| PR-P4 | 🧱 土台・変化なし | 段 7 | 何も変わらない。プラグインが transport を読む・イベントを横取りする拡張点。 |
| PR-P5 | 🧱 土台・変化なし | 段 7 | 何も変わらない。タップ型（LinkAudio をプラグイン化する土台）。 |
| PR-P6 | 🎵 音・操作が変わる | 段 7 | **OSC が送れる**。`var flash = osc("/light/1", 1.0)` を `play(flash, 0, flash)` のようにパターンに置いて、照明などを拍で駆動できる。 |
| PR-P7 | 🧱 土台・変化なし | 段 7 | 振る舞いは同じ。Link テンポがプラグインとして動き、engine は Link を知らなくなる。 |

## PR-K — リリースゲート連鎖（doc 634）

| PR | 見え方 | 段 | 完了するとできること |
|---|---|---|---|
| PR-K-A0 | 📄 仕様・文書 | 段 6 | note-off が鳴る場面（RUN 終端・オフラインレンダ）が仕様に載る。 |
| PR-K-A1 | 🎵 音・操作が変わる | 段 1 | RUN の終端で音が正しく止まる（タイマの取りこぼしと 100 ms のずれが直る）。 |
| PR-K-A2 | 🎵 音・操作が変わる | 段 1 | engine が落ちても note が鳴りっぱなしにならない。 |
| PR-K-C0 | 📄 仕様・文書 | 段 6 | プラグイン遅延補償の v1 でやらないことが仕様に載る。 |
| PR-K-C1 | 🎵 音・操作が変わる | 段 6 | プラグインが申告する遅延が補償され、並列の枝で位相が揃う。 |
| PR-K-C2 | 🎵 音・操作が変わる | 段 6 | mixer の枝間で 1 block ずれていた分が補償される。 |
| PR-K-D0 | 📄 仕様・文書 | 段 6 | 再評価時にレイヤーのラックがどう対応づくかが仕様に載る。 |
| PR-K-D1 | 🎵 音・操作が変わる | 段 6 | `layer()` の枝が並列に走る。 |
| PR-K-G0 | 📄 仕様・文書 | 段 6 | 標準プラグインはカタログに出ない、が仕様に載る。 |
| PR-K-G1 | 🎵 音・操作が変わる | 段 6 | 旧 `compressor` / `limiter` / `normalizer` 語彙が消える（使っている譜面は書き換え。実使用は 1 ファイル 6 行）。 |
| PR-K-G2 | 🧱 土台・変化なし | 段 6 | 何も変わらない。標準 dynamics プラグインの実体（WASM）。 |
| PR-K-G3 | 🎵 音・操作が変わる | 段 6 | `effect([Compressor(...)])` で compressor / limiter を好きな位置に挿せる。 |
| PR-K-I1 | 🎵 音・操作が変わる | 段 6 | instrument ラック。1 つのパターンで複数の instrument を同時に駆動できる。 |

## PR-Q — 時刻付きイベント queue（doc 428）

| PR | 見え方 | 段 | 完了するとできること |
|---|---|---|---|
| PR-Q-A | 📄 仕様・文書 | 段 6 | wire（`ScheduleEvents` / `CancelScheduledEvents`）の仕様が読める。 |
| PR-Q-B | 🧱 土台・変化なし | 段 6 | 何も変わらない。scheduler と block source の時計の乖離を塞ぐ（潜在的なズレの予防）。 |
| PR-Q-C | 🧱 土台・変化なし | 段 6 | 何も変わらない。RT の時刻付き queue。 |
| PR-Q-D | 🧱 土台・変化なし | 段 6 | 何も変わらない。daemon が時刻つきで予約を受ける。 |
| PR-Q-E | 🎵 音・操作が変わる | 段 6 | note が transport 時刻どおりに届く（送信のジッタが音に出なくなる）。 |
| PR-Q-F | 🎵 音・操作が変わる | 段 6 | mute / LOOP を外す / RUN 終端で、予約済みの note が確実に消える。 |
| PR-Q-G | 🎵 音・操作が変わる | 段 6 | `seq.param(...)` でプラグインのパラメータをパターンで動かせる（小節頭でカットオフを変える等）。 |

## PR-D — 診断の整合（doc 610）

| PR | 見え方 | 段 | 完了するとできること |
|---|---|---|---|
| PR-D0 | 🎵 音・操作が変わる | 段 1 | 演奏中に engine が throw で止まらない。鳴らせないイベントは無音でスキップして `[ERROR]` に残る。 |
| PR-D1 | 📄 仕様・文書 | 段 1 | 受け手 × メソッドの適用可否表が仕様に載る。 |
| PR-D2 | 👀 見える | 段 1 | エディタの赤線が engine と同じパーサで出る。「エディタでは通るのに評価で落ちる」が消える。 |
| PR-D3 | 👀 見える | 段 1 | 「この受け手にそのメソッドは効かない」がエディタで警告される。 |
| PR-D4 | 👀 見える | 段 1 | 同名の seq と sum/aux の衝突、aux への誤った出力が赤線 + `[ERROR]` で分かる。黙って上書きされない。演奏は止まらない。 |
| PR-D5 | 🎵 音・操作が変わる | 段 1 | `[1,5,9]@v+10` のように stack に `@v` を書くと voice ごとに分配される。空パターンの案内が出る。 |
| PR-D7 | 🎵 音・操作が変わる | 段 1 | `seq.root("C#4")` のように note 名で root を書ける。 |
| PR-D6 | 👀 見える | 段 1 | 診断が「どの評価から出たか」正しく紐づく。 |

## PR-V — 可視化・設定・性能（doc 662）

| PR | 見え方 | 段 | 完了するとできること |
|---|---|---|---|
| PR-V1 | 🧱 土台・変化なし | 段 5 | 何も変わらない。env の読み方を 1 箇所に。 |
| PR-V2 | 👀 見える | 段 5 | env 変数名の接頭辞が統一される（3 個改名。古い名前は警告つきで読める）。 |
| PR-V3 | 👀 見える | 段 5 | `get_engine_state` に実際のデバイス名・レート・バッファ・callback 統計が出る。stream が止まったことが分かる。 |
| PR-V4 | 🎵 音・操作が変わる | 段 1 | **`--audio-device` / `select_audio_device` で選んだデバイスから本当に音が出る**。 |
| PR-V5 | 👀 見える | 段 5 | MCP からオーディオデバイス一覧と child 一覧が取れる。 |
| PR-V6 | 👀 見える | 段 5 | Engine view にデバイス・ヘッドルーム・ドロップアウト・child の CPU/RSS が出る。 |
| PR-V7 | 👀 見える | 段 5 | 設定一覧（32 変数）が実効値とスコープつきで Studio に出る。 |
| PR-V8a | 🧱 土台・変化なし | 段 5 | 何も変わらない。wake 遅延の計測。 |
| PR-V8b | 🎵 音・操作が変わる | 段 5 | child がアイドル時に CPU を食わない。audio thread の優先度が上がりドロップアウトが減る。 |
| PR-V9 | 📄 仕様・文書 | 段 5 | スレッド・メモリの実測が文書に載る。 |
| PR-V10 | 👀 見える | 段 5 | Studio から MIDI panic とデバイス切替ができる。 |
| PR-V11 | 🎵 音・操作が変わる | 段 5 | プール上限を気にせずプラグイン・render を増やせる（伸長は off-thread）。 |
| PR-V12 | 👀 見える | 段 5 | Engine view が WebviewView になる（見た目と操作の刷新）。 |

## PR-S — 配布（doc 656）

| PR | 見え方 | 段 | 完了するとできること |
|---|---|---|---|
| PR-S-T1 | 👀 見える | 段 1 | untrusted なフォルダでも engine が起動して評価できる（DAW と同じ挙動）。実行ファイルを選ぶ設定 2 つだけ制限される。 |
| PR-S-T2 | 👀 見える | 段 8 | OrbitStudio ビルドでは trust の確認が出ない。 |
| PR-S-R1 | 🧱 土台・変化なし | 段 8 | 何も変わらない。`.vsix` 検査スクリプトの共有。 |
| PR-S-R2 | 🧱 土台・変化なし | 段 8 | 何も変わらない。rust / scripts の変更で CI の smoke が回る。 |
| PR-S-R3 | 🧱 土台・変化なし | 段 8 | 何も変わらない（作る側）。1 コマンドでローカルにリリース成果物が揃う。 |
| PR-S-R4 | 👀 見える | 段 8 | 署名・公証済みの `.app`。Gatekeeper の警告なしに開ける。 |
| PR-S-R5 | 🧱 土台・変化なし | 段 8 | 何も変わらない（作る側）。tag を push すると CI が公開する。 |
| PR-S-C1 | 🧱 土台・変化なし | 段 8 | 何も変わらない。リリース成果物そのものに対して実機 E2E が回る。 |
| PR-S-C2 | 👀 見える | 段 8 | **node が入っていない Mac でも OrbitStudio が動く**（node 同梱）。 |

## PR-E — E2E 基盤（doc 668）

| PR | 見え方 | 段 | 完了するとできること |
|---|---|---|---|
| PR-E0 | 📄 仕様・文書 | 段 0 | E2E の書き方（台帳・観測の種類・変異は critical path に置かない）が文書に載る。 |
| PR-E1 | 🧱 土台・変化なし | 段 0 | 何も変わらない。gated suite の source list を 1 本に。 |
| PR-E2 | 🧱 土台・変化なし | 段 0 | 何も変わらない。共通 helper。 |
| PR-E3 | 👀 見える | 段 0 | `analyze_audio(per_channel: true)` で L/R 別に測れる。`pan` や stem の検証が書ける。 |
| PR-E4 | 🧱 土台・変化なし | 段 0 | 何も変わらない。新しい構文を足して E2E を書かないと red になる。 |
| PR-E5 | 👀 見える | 段 0 | reference の日本語・英語に載っていない語があれば red（docs の網羅が機械で保証）。 |
| PR-E6 | 🧱 土台・変化なし | 段 0 | 何も変わらない。import のエディタ経路 E2E。 |
| PR-E7 | 🧱 土台・変化なし | 段 0 | 何も変わらない。mute / unmute / loop / pan の実機 E2E。 |
| PR-E8 | 🧱 土台・変化なし | 段 0 | 何も変わらない。孤児 daemon の検出。 |
| PR-E9 | 👀 見える | 段 0 | child の deadline 超過時に load average が出て「遅い」と「壊れた」を見分けられる。 |
| PR-E10 | 👀 見える | 段 0 | daemon が起動に失敗した理由（どの段階で・stderr）が見える。 |

## 数え上げ

| 見え方 | PR 数 |
|---|---|
| 🎵 音・操作が変わる | 30 |
| 👀 見える | 25 |
| 🧱 土台・変化なし | 31 |
| 📄 仕様・文書 | 12 |
| 合計 | 98 |

音・操作が変わる PR と見える PR で全体の半分強。残りは土台と仕様で、ユーザーには「同じ音のまま」が保証されるだけの PR。**土台の PR が続く週は「何も変わらない」が正しい状態**なので、進捗はこの表の段で読む。
