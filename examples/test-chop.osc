// Chopテスト - アルペジオを4分割して再生
// 期待される動作：C音のアルペジオが4分割され、1→2→3→4の順で各拍に聞こえる

var global = init GLOBAL
global.tempo(120)
global.beat(4)
global.tick(4)
global.audioPath("/Users/yamato/Src/proj_livecoding/orbitscore/test-assets/audio")

var arp = init global.seq
arp.length(1)
arp.audio("arpeggio_c.wav").chop(4)  // 4つに分割
arp.play(1, 2, 3, 4)  // スライス1→2→3→4の順で再生

// ループ実行
global.run()
arp.loop()
