// Lengthテスト - length(2)で音程が1オクターブ下がる
// 期待される動作：length(1)と比べて各イベントの時間が2倍、rateが1/2になり1オクターブ下がる

var global = init GLOBAL
global.tempo(120)
global.beat(4)
global.tick(4)
global.audioPath("/Users/yamato/Src/proj_livecoding/orbitscore/test-assets/audio")

var arp = init global.seq
arp.length(2)  // 2小節ループ
arp.audio("arpeggio_c.wav").chop(4)
arp.play(1, 2, 3, 4)  // 4つのスライスが2小節に渡って再生される

// ループ実行
global.start()
arp.loop()
