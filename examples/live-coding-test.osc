// Live Coding Test
// Use Cmd+Enter to execute each section

var global = init GLOBAL
global.tempo(120)
global.beat(4 by 4)

// Step 1: Start with kick
var kick = init global.seq
kick.beat(4 by 4).length(4)
kick.audio("../test-assets/audio/kick.wav")
kick.play(1, 0, 1, 0)

// Start playback
global.run()
