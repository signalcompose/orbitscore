// 個別スライステスト - 各スライスを個別にテスト

var global = init GLOBAL
global.tempo(120)
global.beat(4)
global.tick(4)
global.audioPath("/Users/yamato/Src/proj_livecoding/orbitscore/test-assets/audio")

// スライス1のみ
var slice1 = init global.seq
slice1.length(1)
slice1.audio("arpeggio_c.wav").chop(4)
slice1.play(1, 0, 0, 0)

// スライス2のみ
var slice2 = init global.seq
slice2.length(1)
slice2.audio("arpeggio_c.wav").chop(4)
slice2.play(0, 2, 0, 0)

// スライス3のみ
var slice3 = init global.seq
slice3.length(1)
slice3.audio("arpeggio_c.wav").chop(4)
slice3.play(0, 0, 3, 0)

// スライス4のみ
var slice4 = init global.seq
slice4.length(1)
slice4.audio("arpeggio_c.wav").chop(4)
slice4.play(0, 0, 0, 4)

// テスト用コマンド（以下を選択してCmd+Enterで実行）
global.run()
slice1.run()  // スライス1のみ
slice2.run()  // スライス2のみ
slice3.run()  // スライス3のみ
slice4.run()  // スライス4のみ
global.stop()
