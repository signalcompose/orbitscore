// Chopテスト - アルペジオの分割と再生
// 期待される動作：C音のアルペジオが4分割され、1→2→3→4の順で聞こえる

// グローバル設定
var global = init GLOBAL
global.tempo(120)
global.beat(4)        // 4/4拍子
global.tick(4)

// アルペジオシーケンス
var arp = init global.seq
arp.length(1)         // 1小節ループ
arp.audio("../test-assets/audio/arpeggio_c.wav").chop(4)  // 4つに分割
arp.play(1, 2, 3, 4)  // スライス1→2→3→4の順で再生（各拍で1スライスずつ）

// 実行コマンド（以下を選択してCmd+Enterで実行）
// arp.run()

// 停止コマンド
// arp.stop()
// global.stop()
