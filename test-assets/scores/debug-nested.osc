// Debug nested pattern - each slice should sound different

var global = init GLOBAL
global.tempo(60)  // Slow tempo for clear distinction
global.beat(4 by 4)

var test = init global.seq
test.beat(4 by 4).length(1)
test.audio("../../test-assets/audio/arpeggio_c.wav").chop(4)

// Simple pattern: 1, 2, 3, 4 (should be clearly different)
test.play(1, 2, 3, 4)

// Start
global.run()