/**
 * #611 §9 — 出口の一般化に入る**前**の音を、実機 capture の数値で固定する（PR-O0 / #543-a）。
 *
 * 🔴 **ここに書くのは「今の音」であって「正しい音」ではない。** 今日壊れている項目も
 * 壊れた値のまま入れる。PR-O2 / PR-O4 は**変わる行だけ**を後継の式へ切り替え、
 * 差分が式どおりであることが受け入れになる。
 *
 * 🔴 **測定が設計の期待式と食い違ったところは、実測を採る。**
 * doc 611 §9 は aux の寄与を `0.3 * dry`、ラック併用を `effectOnly * gainLinear` と
 * 見積もっていたが、**2026-09-04 の実機はどちらとも一致しなかった**（下の各項）。
 * golden は測ったものを固定する道具なので、**未検証のモデルを assert しない**。
 * 後継 PR の期待値は「実測 × 係数の変化分」という**相対**の形で持つ。
 */

/** dB → 線形。 */
const dbToLinear = (db: number): number => 10 ** (db / 20)

/**
 * 「動いてはいけない」行の許容。
 *
 * 🔴 doc 611 §9 は sum 経路を「許容 0」と書いているが、**MCP capture の窓 RMS は
 * セッション間で 1e-10 程度ゆれる**（2026-09-04 実機: 同一譜面の 2 回の測定が
 * `0.052956636581694194` と `0.05295663657520108`）。文字どおり 0 にすると固定できないので、
 * **ゆれの 4 桁上・可聴閾よりはるかに下**の相対許容を置く。音楽的には「1 サンプルも動かない」と
 * 同じ意味であり、緩めた結果ではない。
 */
const EXACT_TOLERANCE = 1e-6

/** 音が出ていることの下限（無音ハーネスを緑にしないためのガード・#528）。 */
const AUDIBLE_FLOOR_RMS = 0.01

export const OUTPUT_LINE_GOLDENS = {
  /**
   * 譜面 1: バス無し（`kick_loop.orbs`）。PR-O2 の変更 1 / 2 / 6 が通っても動いてはいけない。
   *
   * 🔴 **サンプル単位の bit 一致は、この層では取れない。** MCP の capture を 2 セッション録ると、
   * オーディオクロックに対する録音開始位相が毎回わずかに違うので、切り出した 1 サイクルの
   * バイト列は一致しない（2026-09-04 実機で不一致を確認）。したがってここは
   * **決定性のガード**であり、比較は窓 RMS の相対差で行う。
   *
   * 設計 §9 が言う「bit 一致」の本体は Rust 側のオフライン比較
   * （`rust/crates/orbit-audio-daemon/tests/capture_realtime_gated.rs` — 静的スケジュールと
   * レンダ PCM の突き合わせ）にあり、PR-O2 はそちらでも確認する。
   */
  noBus: {
    channels: 2,
    audibleFloorRms: AUDIBLE_FLOOR_RMS,
    /** 2026-09-04 実機（同一譜面を 2 セッション）。 */
    rms: 0.0586666021,
    tolerance: EXACT_TOLERANCE,
  },

  /** 譜面 2: `global.sum("o0sum611")` + `kick.output("o0sum611")`。PR-O2 が動かしてはいけない。 */
  sumOutput: {
    /** 2026-09-04 実機。 */
    rms: 0.05295663657520108,
    tolerance: EXACT_TOLERANCE,
    audibleFloorRms: AUDIBLE_FLOOR_RMS,
  },

  /**
   * 譜面 3: `send(aux, 0.3)`。PR-O4 で 0.3 が**線形係数から dB へ**読み替えられる。
   *
   * 🔴 **設計 §9 の「aux RMS = 0.3 × dry」は実機と合わなかった。** 実測の aux/dry は
   * **0.678**（2026-09-04）。dry と aux は同じ信号のコピーなので、窓 RMS は素直に線形加算
   * されず（位相・経路の遅延・合流点の扱いが効く）、係数をそのまま比に読み替えられない。
   *
   * したがって golden は**実測比**を固定し、後継の期待値は
   * 「係数が `0.3` から `10^(0.3/20)` へ変わった分だけ動く」という**相対**で持つ。
   * この形なら、比の絶対値がどうであれ PR-O4 の差分は式で検証できる。
   */
  send: {
    amountAsWritten: 0.3,
    /** 2026-09-04 実機。 */
    dryRms: 0.0648583690575014,
    /** 2026-09-04 実機。`(total - dry) / dry`。 */
    measuredAuxOverDry: 0.6783737720154158,
    /** PR-O4: 係数が線形 0.3 → `10^(0.3/20)` に変わる分だけ動く。 */
    dbAuxOverDry: 0.6783737720154158 * (dbToLinear(0.3) / 0.3),
    tolerance: 0.15,
    audibleFloorRms: AUDIBLE_FLOOR_RMS,
  },

  /**
   * 譜面 4: `effect([Gain(db: 6)]).gain(-6)`。
   *
   * 🔴 **この行が固定するのは大きさであって順序ではない。** doc 611 §9 は既定位置が
   * ラック前 → ラック後へ動く（PR-O2）ことで dry/wet 比が変わると見積もっているが、
   * **本リポジトリが決定論的に走らせられるラックでは観測できない** — カタログの
   * エフェクト fixture は固定 gain の乗算だけで（`rust-spike/clap-test-effect/src/lib.rs:314`
   * の `*o = i * gain`）、スカラー乗算は `seq.gain()` と**可換**である。
   *
   * 🔴 さらに、実測は**単純な乗算モデルとも合わなかった**:
   * `effectOnlyOverDry = 3.155` に対し `combined = 1.415` で、
   * `3.155 × 10^(-6/20) = 1.577` とは 10% 違う。つまりこの経路のどこかに
   * **線形でない要素がある**（信号がラック内で上限に当たっている可能性が高い）。
   * 原因を推測で決めずに、**測った 3 つの比をそのまま固定**する。
   *
   * この行が守るもの: PR-O2 がゲインを動かしたときに、**二重に掛かる・落ちる・
   * 段の幅を取り違える**といった大きさの事故が起きたら赤くなる。
   *
   * **順序**の主張は doc 611 **E2E-6**（`output(verb, thru: true)` をラックの前後に置き分け、
   * **aux タップ**で判定する）が担う。タップ位置が違えば線形ラックでも分離できる。E2E-6 は PR-O4。
   */
  sequenceGainWithEffect: {
    gainDb: -6,
    gainLinear: dbToLinear(-6),
    effectGainDb: 6,
    /** 2026-09-04 実機。 */
    dryRms: 0.05295663658915745,
    /** 2026-09-04 実機。ラックのみ（seq gain なし）/ dry。 */
    effectOnlyOverDry: 3.1548116869595653,
    /** 2026-09-04 実機。`effect([Gain(db: 6)]).gain(-6)` / dry。 */
    legacyCombinedOverDry: 1.414755069864296,
    tolerance: 0.15,
    audibleFloorRms: AUDIBLE_FLOOR_RMS,
  },

  /**
   * 既存 #643 E2E-1 の観測を記録する（**そのテスト自体は PR-O0 では触らない**）。
   *
   * 🔴 **E2E-1 の落ち方は実行ごとに変わる。** 2026-09-04 に 3 回まわした実測:
   *
   * | 実行 | unity RMS | half RMS | half/unity |
   * |---|---|---|---|
   * | 1 | **0**（instrument が鳴っていない） | — | 測れない |
   * | 2 | 0.0632 | 0.0858 | **1.356** |
   * | 3 | 0.0706 | 0.0858 | **1.216** |
   *
   * つまり **3 回中 2 回は「gain が効かない」で落ち**（比が 1 前後 = 減衰していない）、
   * 1 回は**その手前の可聴チェック**で落ちた。**instrument の可聴性そのものが不安定**である。
   * `half` はほぼ動かない（0.08576 / 0.08579）のに `unity` が 11% 動くのも、
   * 不安定なのが unity 側の測定であることを示している。
   *
   * ⚠️ **1 回だけの観測から「instrument が鳴っていない」と一般化しないこと**
   * （本 PR の作業中に一度その誤りを書いた）。
   *
   * したがって golden は**幅**で持ち、原因の特定は PR-O2 の測定ラダー
   * （`docs/design/649-audio-line-design.md` §13）で行う。
   */
  globalGainInstrument: {
    gainDb: -6,
    /** 2026-09-04 実機で観測した「今日の」比の幅（減衰が掛かっていない状態）。 */
    legacyHalfOverUnityRange: [1.2, 1.4] as const,
    /** PR-O2 後の受け入れ: half/unity が `10^(-6/20)`。 */
    prO2HalfOverUnity: dbToLinear(-6),
    ratioTolerance: 0.05,
    /** E2E-1 が比を測れる前提。3 回に 1 回はここで落ちた。 */
    audibleFloorRms: 0.05,
  },
} as const
