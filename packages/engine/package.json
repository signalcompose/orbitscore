{
  "name": "@orbitscore/engine",
  "version": "0.0.1",
  "type": "commonjs",
  "main": "dist/index.js",
  "bin": {
    "orbitscore": "./dist/cli-audio.js"
  },
  "scripts": {
    "build": "tsc --build && node scripts/sync-dist.js",
    "build:clean": "tsc --build --clean && tsc --build && node scripts/sync-dist.js",
    "dev": "ts-node src/index.ts",
    "test": "npx vitest run --dir ../../tests --globals --pool=forks --poolOptions.forks.singleFork=true",
    "cli": "cd ../.. && node packages/engine/dist/cli-audio.js"
  },
  "dependencies": {
    "supercolliderjs": "^1.0.1",
    "wavefile": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "vitest": "^2.1.9"
  }
}
