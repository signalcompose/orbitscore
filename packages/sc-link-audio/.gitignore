# CMake build output
build/
cmake-build-*/

# Plugin artifacts (regenerated by CI / local build, not tracked in VCS)
*.scx
*.so
*.dll

# Editor scratch
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# external_libraries/ submodules are added in Step 2.2 — until then, keep the
# checked-in placeholder (.gitkeep) but ignore any local clones.
external_libraries/supercollider-sdk/
external_libraries/link/
